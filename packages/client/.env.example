# Vite (.env) example for @game/client
# Copy to packages/client/.env and adjust as needed. Do not commit real values.

# Feature flags
# Turn on the new Pixi-based map (map-next) on Galaxy page (feature-gated swap)
# Accepts 'true' or 'false'. Default is off.
VITE_FEATURE_MAP_NEXT=true

# Dev-only HUD for Map Next (shows FPS, zoom, layer counts). Default off.
# Accepts 'true' or 'false'.
VITE_DEBUG_MAP_NEXT=false

# API configuration (optional overrides)
# VITE_API_URL=http://localhost:3001/api
# VITE_PRODUCTION_HOST=attrition-game.onrender.com
# VITE_ENVIRONMENT=development
# VITE_FORCE_DEV_MODE=false

// Jest setup file

// Load environment variables if needed (optional). In this package we avoid external deps in setup.

// Add any global test setup here
beforeAll(() => {
  // Setup any test prerequisites
});

afterAll(() => {
  // Cleanup after all tests
});

// Reset mocks after each test
afterEach(() => {
  jest.clearAllMocks();
});

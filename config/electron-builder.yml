appId: com.attrition.game
productName: Attrition
copyright: Copyright © 2025 ${author}

# Ensure Windows artifact matches updater/latest.yml expectations
artifactName: "Attrition-Setup-${version}.${ext}"

# Disable code signing globally
forceCodeSigning: false

directories:
  output: ../releases
  buildResources: ../build

files:
  - "src/**/*"
  - "node_modules/**/*"
  - "package.json"
  - "!**/.vscode/*"
  - "!electron.vite.config.{js,ts,mjs,cjs}"
  - "!{.eslintignore,.eslintrc.cjs,.prettierignore,.prettierrc.yaml,dev-app-update.yml,CHANGELOG.md,README.md}"
  - "!{tsconfig.json,tsconfig.node.json}"
  - "!{.env,.env.*,.npmrc,pnpm-lock.yaml}"
  - "!**/__tests__/**"
  - "!**/*.test.js"

extraResources:
  - from: "../../packages/client/dist"
    to: "client"
    filter: ["**/*"]
  - from: "../launcher"
    to: "launcher"
    filter: ["Attrition Launcher.exe"]

asarUnpack:
  - resources/**

win:
  executableName: Attrition
  # icon: build/icon.png  # Temporarily disabled for testing
  publisherName: Attrition Game Studio
  sign: null
  signAndEditExecutable: false
  signDlls: false
  target:
    - target: nsis
      arch:
        - x64

nsis:
  oneClick: false
  perMachine: false
  allowToChangeInstallationDirectory: true
  deleteAppDataOnUninstall: true
  createDesktopShortcut: false  # We'll create launcher shortcuts instead
  createStartMenuShortcut: false  # We'll create launcher shortcuts instead
  displayLanguageSelector: true

mac:
  # icon: build/icon.png  # Temporarily disabled for testing
  category: public.app-category.games
  target:
    - target: dmg
      arch:
        - universal
    - target: zip
      arch:
        - universal

dmg:
  title: "${productName} ${version}"
  artifactName: "${productName}-${version}-${arch}.${ext}"
  # background: build/background.png  # TODO: Create DMG background image
  contents:
    - x: 410
      y: 150
      type: link
      path: /Applications
    - x: 130
      y: 150
      type: file

linux:
  # icon: build/icon.png  # Temporarily disabled for testing
  category: Game
  target:
    - target: AppImage
      arch:
        - x64
    - target: deb
      arch:
        - x64

appImage:
  artifactName: "${productName}-${version}-${arch}.${ext}"

deb:
  packageCategory: games
  priority: optional
  depends:
    - gconf2
    - gconf-service
    - libnotify4
    - libappindicator1
    - libxtst6
    - libnss3

publish:
  provider: github
  owner: BrianSMitchell
  repo: attrition-desktop
  publishAutoUpdate: true

# Auto-updater configuration
electronUpdaterCompatibility: ">= 0.1.0"
compression: maximum
generateUpdatesFilesForAllChannels: true

# Enable blockmap for delta updates

# Attrition - Root .env.example
# DO NOT PUT REAL SECRETS IN THIS FILE. Copy to ".env" locally and fill values.
# Ensure ".env" is gitignored (already configured in .gitignore).

# X / Twitter API (used by tools/python/x_post.py)
# Required (OAuth 1.0a user context)
X_API_KEY=
X_API_SECRET=
X_ACCESS_TOKEN=
X_ACCESS_TOKEN_SECRET=

# Optional (not required by the Python CLI above, but provided for completeness)
X_BEARER_TOKEN=
X_OAUTH2_CLIENT_ID=
X_OAUTH2_CLIENT_SECRET=

# Usage:
# 1) Copy this file to ".env" (same directory).
# 2) Fill in values locally (never commit ".env").
# 3) Activate your venv and run with --use-dotenv:
#    python tools/python/x_post.py --message "Text" --use-dotenv

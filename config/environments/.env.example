# Attrition - Root .env.example
# DO NOT PUT REAL SECRETS IN THIS FILE. Copy to ".env" locally and fill values.
# Ensure ".env" is gitignored (already configured in .gitignore).

# X / Twitter API (used by tools/python/x_post.py)
# Required (OAuth 1.0a user context)
X_API_KEY=
X_API_SECRET=
X_ACCESS_TOKEN=
X_ACCESS_TOKEN_SECRET=

# Optional (not required by the Python CLI above, but provided for completeness)
X_BEARER_TOKEN=
X_OAUTH2_CLIENT_ID=
X_OAUTH2_CLIENT_SECRET=

# AI Service Configuration (used by AI-enhanced social media posting)
# Required - OpenAI API key for content generation
OPENAI_API_KEY=

# Optional AI Configuration (defaults provided in config/ai-config.js)
# AI model to use (default: gpt-4o-mini)
AI_MODEL=

# Content generation settings
AI_MAX_TOKENS=         # Max tokens per request (default: 500)
AI_TEMPERATURE=        # Creativity level 0.0-2.0 (default: 0.7)
AI_RATE_LIMIT=         # Requests per minute (default: 60)

# Context analysis settings  
AI_CONTEXT_DAYS=       # Days of git history to analyze (default: 7)
AI_MAX_COMMITS=        # Max commits to analyze (default: 50)
AI_CACHE_ENABLED=      # Cache context analysis (default: true)

# Content validation settings
AI_MAX_TWEET_LENGTH=   # Maximum tweet length (default: 280)
AI_MIN_TWEET_LENGTH=   # Minimum tweet length (default: 10)

# Usage:
# 1) Copy this file to ".env" (same directory).
# 2) Fill in values locally (never commit ".env").
# 3) For X posting: Activate your venv and run with --use-dotenv:
#    python tools/python/x_post.py --message "Text" --use-dotenv
# 4) For AI-enhanced posting: Run with AI mode:
#    node scripts/social-post.js --ai-mode
